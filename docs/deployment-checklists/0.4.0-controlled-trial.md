# Deployment Readiness Checklist — 0.4.0 Controlled Trial

> Verified on unique temporary databases with `trap`-guaranteed cleanup
> and strict hash validation. **Not a declaration of "deployable"** —
> this is a controlled-trial gate report.

## Owner decisions (confirmed)

- **First deployment is controlled trial only.** P0-1/P0-2 require
  end-to-end design before authoritative data collection.
- **Version remains 0.4.0.**
- **No pre-squash database with data that must be preserved.** Production
  uses a fresh empty database.

## Gate databases (unique temporary, trap-guaranteed cleanup)

| Database | Role | Created | Dropped | Verified gone |
|---|---|---|---|---|
| `perovskite_gate_a54d6159` | Gate verification | exit 0 | trap, exit 0 | DROP_OK=1 |
| `perovskite_restore_a54d6159` | Backup restore target | exit 0 | trap, exit 0 | DROP_OK=1 |

Credentials read from `$LOCALAPPDATA/PerovskiteWorkflow/test-db.env` via
`PGPASSWORD` env var — never echoed, logged, or committed.

The `trap cleanup EXIT INT TERM ERR` handler:
1. Disarms traps (prevents recursion)
2. `pg_terminate_backend` for all non-self connections to both databases
3. `DROP DATABASE IF EXISTS` for each
4. Queries `pg_database` to verify each is gone
5. If any verification fails on the success path (exit code 0), forces
   non-zero exit

## Verification commands and exit codes

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | CREATE gate DB | 0 | `perovskite_gate_a54d6159` |
| 2 | `alembic upgrade head` | 0 | 0014 applied |
| 3 | `alembic check` | 0 | No drift |
| 4 | `PEROVSKITE_VITE_SMOKE=1 python -m unittest discover -s tests` | 0 | `OK` — 0 failures, **0 skipped** |
| 5 | `python -m unittest tests.test_postgresql_integration` | 0 | `OK` |
| 6 | Concurrency 3 groups ×5 | 0 | 15/15 OK |
| 7 | Migration cycle + 0013 blocked + 0014 assignment repair | 0 | 3/3 OK |
| 8 | `pg_dump` | 0 | 3,368,458 bytes |
| 9 | CREATE restore DB + restore | 0 | Restored |
| 10 | Per-table content hash (19 tables, strict MD5 validation) | 0 | All MATCH |
| 11 | Revision + index comparison | 0 | MATCH / MATCH (101) |
| 12 | DROP gate DB (trap, verified gone) | 0 | DROP_OK=1 |
| 13 | DROP restore DB (trap, verified gone) | 0 | DROP_OK=1 |

**Script exit code: 0.**

## Backup

- **Source**: `perovskite_gate_a54d6159` (unique temporary)
- **Size**: 3,368,458 bytes
- **SHA-256**: `26b54b38403de21d1e6b57d62b7737eacd868457cc2c9da01254156e3b96a998`

## Per-table content hash

`md5(string_agg(row::text ORDER BY row::text))` — every column of every row.
Each hash is validated as a 32-character MD5 hex string before comparison;
any query failure or invalid output causes immediate non-zero exit.

All 19 tables: **MATCH**.

| Table | Hash prefix |
|---|---|
| users | 8abe55b13d37… |
| experiments | 611a9bfacd1c… |
| fabrication_batches | 2523e6b39c65… |
| fabrication_batch_conditions | 964432281870… |
| fabrication_substrates | e9302928ae12… |
| fabrication_devices | ca1868d19143… |
| solution_preparations | cdd98d3a9a15… |
| solution_preparation_uses | 7b0f15d50a70… |
| process_executions | bab784772820… |
| process_execution_members | 85cd6806d0e1… |
| execution_deviations | 33b47a53dd2e… |
| result_files | bd14ce1bbb10… |
| result_device_assignments | 764934e24031… |
| audit_events | 4596d2a98e97… |
| baselines | 9743f9ffad80… |
| layer_presets | a895bf04342e… |
| materials | 71d09a219b26… |
| campaigns | bc6f7518ae09… |
| device_layout_versions | 5e12c23a3204… |

## Schema fingerprint

- **Alembic revision**: MATCH (`0014_deployment_integrity_repairs`)
- **Indexes**: MATCH (101)

## Wheel

- `perovskite_deposition_bo-0.4.0-py3-none-any.whl`
- SHA-256: `166d7cc39777a96d1e8dff45104567bab814c0c0deca28b5fe4608f2d3329705`

## Static artifact

- JS: `index-B85IS-9T.js` — `f5326bf2004c2988ee34ce16ca9a34c3c14ff68289c580812f0c466899422dcb`
- CSS: `index-RnzBLjWX.css` — `c40254ccb0b0bc068aba9ee2382c2073fd003b0549adc6de1796850871303755`

## Skip names (without VITE_SMOKE)

7 `test_vite_proxy_smoke.*` tests — opt-in `PEROVSKITE_VITE_SMOKE=1`.
With it set: **zero skips.**

## P0-1 / P0-2 gate

**BLOCKED.** Authoritative data collection must not begin until P0-1
(environment capture) and P0-2 (material lot provenance) designs are landed.

## 0013 downgrade boundary

Intentionally blocked with `RuntimeError` before any DDL or DML.

## Backend: full-condition pre-check before any write

`_record_actual_substrate_counts` runs a **pre-check pass** over ALL
conditions (actual counts, shortfall deviations, required layer execution
groups, AND no-groups-at-all check) before the **write pass**. A failure
in any condition's pre-check prevents any write for any condition.

## Deletion evidence

```
cleanup: GATE_DB=perovskite_gate_a54d6159 RESTORE_DB=perovskite_restore_a54d6159 backup=... all removed (DROP_OK=1)
GATE_EXIT=0
```

## Verification script

`scripts/verify-gate-db.sh` — creates unique gate and restore databases,
runs all verification, validates each hash as 32-char MD5 (immediate
non-zero exit on any query failure or invalid output), terminates
connections before DROP, verifies databases are gone, and forces non-zero
exit if cleanup fails on the success path.

## Final controlled-trial artifact (post-remediation merges)

The hashes above record the gate run at `e018422`, before the
deployment-readiness CI fixes (PR #12, merged as `5c9925d`) and the
gap-remediation work (PR #13, merged as `18c54e8`) landed on `main`.
The deployable artifact for the controlled trial is the wheel and static
bundle rebuilt from the final `main` commit `18c54e8`, verified on
2026-08-24 per DEPLOYMENT.md section 1:

- `uv lock --check` — pass (63 packages resolved from the lockfile).
- `uv sync --locked --extra test --no-dev --no-editable` — pass.
- `uv build` — pass.
- `tests.test_installed_wheel_smoke` against the new wheel — pass
  (0 failures).
- GitHub Actions on `main` merge refs: unittest matrix (ubuntu 7m59s,
  windows 22m49s), postgresql (59s), frontend (4m3s) — all green for
  PR #13; PR #12 passed the same four jobs at `2339794`.

Artifact hashes (SHA-256):

| Artifact | Hash |
| --- | --- |
| `perovskite_deposition_bo-0.4.0-py3-none-any.whl` | `4659eafeabb8d6064eca084d4c28fe4664d72d44b367ce421b4a7f86ef6593c6` |
| `src/web/static-app/assets/index-Bo7O3crE.js` | `023ccfa015c774ad159545859cf5e506ef9afd0eb434c427c3c06d86dec69fe6` |
| `src/web/static-app/assets/index-RnzBLjWX.css` | `1e4ebaf9ae99913f9abd2c87faf0558a3826f27356792d4653596772aebbf398` |

The database-level verification (migration cycle, dump/restore,
per-table hashes) is unchanged by these merges — no migration files were
added or altered after `0014`, so the original gate evidence remains
valid for the schema.
